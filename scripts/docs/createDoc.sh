#!/bin/sh

#dir=$(dirname "${BASH_SOURCE[0]}") #just get the directory portion of the path

dir=$(dirname $0)
echo $dir

#run jsdoc with the jsconf and the readme, might need to create a separate readme for jsoc s
###