/**
 * @todo have another function performChecks() which will call 
 * checkApiQuota(), checkStorage() capacity of destination account.
 * 
 * Api quota will have formula to be able to calculate or at least estimate 
 * the capacity and quota needs for each transfer
 */
 function checkApiQuota()
 {
     console.log('\x1b[36m%s\x1b[0m', 'I am cyan');  //cyan
     process.stdout.write("text");
     process.stdout.clearLine();
     process.stdout.write("more");
     process.stdout.write("\n"); // end the line
 }
 
 function checkStorage()
 {
     
 }
 